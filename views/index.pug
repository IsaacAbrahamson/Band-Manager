html
  head
    title= title
    rel(rel="stylesheet" href="/stylesheets/style.css")
  body
    img(src='/images/test.jpg')
    h1= message
    p This application executes a simple query on a MySQL database.
    table
      each row in database
        tr
          td= row.id
          td= row.time
          td= row.theme