html
  head
    title= title
    link(rel="stylesheet" type="text/css" href="/stylesheets/style.css")
  body
    h1= message
    p This application executes a simple query on a MySQL database.
    table
      each row in database
        tr
          td= row.id
          td= row.time
          td= row.theme
          td
            a(href='/service/' + row.id) Explore